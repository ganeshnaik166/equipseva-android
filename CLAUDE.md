# EquipSeva continuation

Read and follow [AGENTS.md](AGENTS.md), then [docs/CURRENT_STATE.md](docs/CURRENT_STATE.md), before starting or resuming work. The current-state record identifies the active app branch, exact evidence, open failures and next slice. Read its linked plan and handoff; verify against Git and source.

Do not assume this checkout or private Claude memory contains the latest work. Preserve other worktrees and the shared build reservation. At each milestone/checkpoint update the portable repository records and supported project memory as required by AGENTS.md. Never store secrets or copy private memory into Git.
