# Handoff — 2026-09-07 06:45 UTC (branch `ops/r1388-calendar-burndown`)

Supersedes `HANDOFF_2026-09-07.md` (04:15 UTC). Session detail for rounds 3816–3820 lives in
`docs/SESSION_REFRESH_round3816.md`.

## State
- Working tree clean; every commit pushed to `origin/ops/r1388-calendar-burndown`. Android CI + secret-scan **green** on `3f4d5213`.
- Prod migrations applied through **round3819** (`supabase db push --dry-run` → "Remote database is up to date").
- Local verify bar on the committed tree: BUILD SUCCESSFUL — 2791 unit tests / 0 failures, lintDebug, assembleDebug.
- Emulator `eqs`: r3816 debug APK installed, signed in as play-review-hospital (session refreshed by the SDK at 06:39 UTC).
- Standing: revoke the Supabase access token at `/c/Users/lokes/eqs-token.txt` when done.
- A second Claude session is working in the SAME checkout (it committed `docs/UX_UPLIFT_PLAN.md`, `e5fc5c4d`). Fetch before
  committing; never run two Gradle builds at once.

## Shipped this session (all on `ops/r1388-calendar-burndown`)
| round | commit | what |
|---|---|---|
| 3816 | `cb9142af` | supabase-kt 3.0.3 → 3.6.0 (ktor 3.4.3, Kotlin 2.3.21): sessions refresh after the 3600 s JWT. **Proven on device** — see below. Also fixed a real regression the upgrade would have shipped (RestException.message embeds `URL:` → DataError pass-through collapsed). |
| 3817 | `cb9142af` | Check-in / Mark-done / Revise-quote / Cancel only for the ASSIGNED engineer (`isViewerAssignedEngineer`). |
| 3818 | `cb9142af` (db) | GPS check-ins → `evidence_ledger` (`gps_checkin`), `engineer_attendance.evidence_ledger_id` populated. The ledger had **0 rows since round492**. |
| 3819 | `5b09bb7b` (db) | DSR engineer attestation + hospital countersign → ledger, chained by `attests_sha256`. |
| 3820 | `3f4d5213` | Before/after photos → `register_evidence` via new `evidence_register` outbox kind (client sha256 of the stored bytes). SDK log level back to INFO (DEBUG leaks refresh tokens). |

## r3816 idle test — PASSED (06:39 UTC)
Sign-in 04:50:52 UTC → timer refresh 05:38:51 (idle, foreground) → the NEXT scheduled refresh (06:26:54) silently did not run,
exactly the original failure class → first request after expiry at 06:39:04 triggered the SDK's request-time force-refresh
(`/auth/v1/token` 200) and every screen loaded. 0 crashes. Full table in `docs/SESSION_REFRESH_round3816.md`.

## Cron ledger (Task 2)
`cron_tick_runs` ids 3–8: six consecutive green **hourly** ticks (latest 04:26:50 UTC today, 8 slots, 0 failed). The **daily**
tick had not fired by 06:42 UTC (expected ~07:30–08:00 UTC) — re-check with
`select id, slot, ok, created_at, failed_slots from public.cron_tick_runs order by id desc limit 12` via
`supabase.exe db query --linked -f file.sql` (the founder-gated `founder_cron_tick_recent()` 42501s from the CLI).

## DONE 06:55 UTC — `main` brought to the shipped product (founder picked option 1)
- Old main tip `d72661c6` (r1516) tagged **`archive/main-r1516-deferred-features`** (annotated, pushed).
- Merge commit **`f33effff`** = `-s ours` from the ops side: tree identical to ops `39618c95` (verified `git diff --stat` = 0
  lines), all 137 old-main commits still reachable from main (`merge-base --is-ancestor` OK).
- `origin/main` = `origin/ops/r1388-calendar-burndown` = `f33effff`; main..ops = ops..main = 0. Local checkout fast-forwarded
  with no file changes (tree identical). Scratch worktree removed.
- Workflows triggered on main by the push: android (pending), web (in progress — web/ still carries its known ~9.7 k TS
  errors, so a red `web` run is old debt, not a live incident), secret-scan ✅, GitHub Pages build+deploy ✅ (Pages serves
  `docs/` from main — the hosted password-reset page came along). `cron-tick-code-red.yml` (*/5) is now live on main.
- From here on `main` and `ops/r1388-calendar-burndown` are the same line; the "never work on main" rule is obsolete — keep
  shipping on ops and fast-forward main, or ship on main directly; either way keep them equal.

Facts that were measured before acting (kept for the record):
- `origin/main` = `d72661c6` (r1516). It has **137 commits that exist nowhere else** (r1387–r1516: the deferred-features batch;
  their *fixes* were already ported to ops, the 59 *features* were deferred by standing decision).
- `ops/r1388-calendar-burndown` has **689 commits not on main**. Merge-base `0950a541` (r1386).
- A real merge (`git merge origin/main` in a scratch worktree `C:/Users/lokes/eqs-merge`, aborted) has **86 conflict hunks in
  33 files**: JobProfitabilityScreen 14, ProfileScreen 7, EarningsScreen 5, MyBidsScreen 5, RepairJobDetailScreen 4,
  MainNavGraph 4, RepairJobDetailViewModel 4, OrderSummarySheet 4, FounderEngineerPayoutsScreen 4, DataError 3, …
- `main` is not branch-protected. Nothing deploys from `main` automatically (web is manual `vercel --prod`; Android release is a
  manual workflow), but merging ops into main would activate `cron-tick-code-red.yml` (*/5) and the other 6 workflow changes.
Options (NOT executed — needs the founder's call):
1. **Make `main` = the shipped product** (recommended): tag the current main tip `archive/main-r1516-deferred-features`, then
   `git merge -s ours origin/main` from ops (history of the 137 commits kept; tree = ops) and push to main. Zero conflict risk;
   the 59 deferred features stay recoverable from the tag.
2. **Full 3-way merge**: resolve 86 hunks by hand, run the bar, push. Hours; semantic risk; the features' SQL may also need
   porting to prod.
3. Force-push ops over main — discards the 137 commits. Not recommended.

## Also queued
- On-device drive of r3820 (a check-in with before-photos through the system picker → expect `photo_before` ledger rows).
- Founder decisions unchanged: payment rail (launch-blocking), FCM push runbook, merge `ops/main-workflow-fixes` (moot if main is
  brought to ops), main's Dependabot vulns.
