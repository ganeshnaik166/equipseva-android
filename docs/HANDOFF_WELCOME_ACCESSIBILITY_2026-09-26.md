# Welcome accessibility handoff — 26 September 2026

## Resume point and ownership

Worktree: `C:/Users/lokes/Documents/Codex/2026-09-07/im/work/equipseva-welcome-20260926`; branch: `codex/welcome-accessibility-20260926`; [PR1888](https://github.com/ganeshnaik166/equipseva-android/pull/1888). Started from fetched `origin/main` `a645a59300c9ab1ec9ce93a16efdcdf3ef6f8f8b`, then merged accepted main `3c5f8b6c71275c06fc3ec1da55895cc9cae745c8`. Original app source/test commit **`ce8ac65972b34eaf46a6fa5ef372770589bac1f0`** was verified locally. Push CI then found a design-lint failure, so source/style/resources were corrected at **`9abd0ffc5056be1ff612ee6d6e712b8e6f0f7c8b`**. Its blobs are `WelcomeScreen.kt` `9f100eab2bbfcc15cf0ae3e7e6ec33ba5f987985`, `Type.kt` `2e1715780e9fcd7e87db4c5885ab01ee75187018`, `strings.xml` `0cc7b1af981236eac1dec630484564489ab34b15`, and unchanged `WelcomeScreenUiTest.kt` `50d51d92d31efa435842b56a59b54f36d394b1ec`. Fresh Android verification of this corrected code passed as recorded below.

This slice owns only the Welcome screen, a new focused Robolectric Compose test, and these progress records. It makes the page scroll at large text/small height; puts the two account actions and separate, 48dp Terms/Privacy targets in the compact first view; keeps the agreement next to the actions; strengthens the Create account outline; and avoids saying the brand twice to a screen reader. The public `WelcomeScreen(onSignIn,onSignUp)` contract and existing legal URLs remain. No auth routing, permission, backend, locale or P1d token/sender file changed.

## Test and review evidence

- Test-first RED on the old layout: focused Welcome tests **5/2** (offscreen Sign in and duplicate logo name). After initial layout fix, focused **5/0**. QA then identified legal copy below long marketing text; its new order assertion was **5/1** RED before the move and **5/0** after.
- Temporary synthetic Roborazzi probe test rendered normal and 320×420dp/200%-font compact states, including a scrolled bottom view. Probe runs passed. The test was removed and the PNGs remain outside Git at `outputs/welcome-probe-compact-fixed.png` and `outputs/welcome-probe-compact-bottom.png`. These are reviewed local images, not committed goldens or a Welcome screenshot CI gate.
- Original `ce8ac659` combined Gradle command: `:app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleRelease --continue --no-daemon --console=plain`, with Git Bash on Windows PATH and `PRECHECK_LOOSE=1`; exit **0**, `BUILD SUCCESSFUL in 7m 17s`. Test XML: **342 suites, 2,912 tests, 0 failures, 0 errors, 0 skipped**; Welcome class **5/0**. Lint XML: **0 errors, 87 warnings, 2 other**. Debug and unsigned R8 release assembly passed. Local log: `C:/Users/lokes/Documents/Codex/2026-09-07/im/outputs/welcome-full-final-20260926.log`. These figures do **not** verify corrected `9abd0ffc`.
- PR1888 initial push Android CI failed the existing `scripts/verify/design_lint.py` ratchet: `raw_dp +13`, `raw_sp +6`, `font_size_assign +2`, `hardcoded_text +1`. The correction uses existing spacing/radius tokens, exact Welcome type styles and English string resources. Local design lint on `9abd0ffc` exits **0**: `raw_dp -2`, `raw_sp -6`, `font_size_assign -4`, `hardcoded_text 0` relative to baseline. The baseline was not changed or suppressed.
- Corrected `9abd0ffc` focused run: Welcome tests **5/0**, plus two temporary synthetic visual-probe tests **2/0**. New compact/normal images `outputs/welcome-probe-compact-token-fixed.png` and `outputs/welcome-probe-normal-token-fixed.png` were compared to prior probes by coordinator, critic and QA. The temporary probe source was removed before the final full run and is not committed; no Welcome golden is present in CI.
- Corrected `9abd0ffc` final combined command: `:app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleRelease --continue --no-daemon --console=plain`, Git Bash on Windows PATH, `PRECHECK_LOOSE=1`; exit **0**, `BUILD SUCCESSFUL in 6m 54s`. Test XML: **342 suites / 2,912 tests / 0 failures / 0 errors / 0 skipped**, Welcome **5/0**. Lint XML: **0 errors / 87 warnings / 2 other**. Debug and unsigned R8 release assembly passed. Log: `C:/Users/lokes/Documents/Codex/2026-09-07/im/outputs/welcome-token-full-20260926.log`. Final-head hosted CI still needs to run.
- The first combined attempt failed release configuration without `PRECHECK_LOOSE=1`; the next attempt reached unit/lint/debug but release precheck could not launch `bash` on the original Windows PATH. The final run corrected the environment, not the security guard. Loose precheck warned that local `EXPECTED_CERT_SHA256` and release keystore are missing, while live assetlinks checks passed. `app-release-unsigned.apk` is not publishable. Crashlytics logged mapping upload; no independent provider receipt was checked.
- Independent [critic](helper-reviews/codex-20260926/welcome-accessibility-critic.md) **9.7/10** and [QA](helper-reviews/codex-20260926/welcome-accessibility-qa.md) **9.6/10**, each for corrected `9abd0ffc` and the bounded Welcome accessibility scope only. Both initially held the hidden compact CTA/duplicate label and accepted after the fixes; QA also held then accepted legal-copy ordering. They reviewed both final probes but did not independently run Gradle.

## Remaining gates and next actions

Fetch and push the corrected branch to open PR1888; require applicable Android/secret-scan/screenshot-workflow CI on its final head before main integration. The current Roborazzi workflow lacks a Welcome golden; do not describe a green screenshot job as visual regression coverage for this page. Physical device/TalkBack, intermediate-height exploration and actual external-intent execution remain. The global Space Grotesk/Inter font target, four-persona registration and three-choice entry plan are separate wider UI work, not completed here.

QA reproduced a pre-existing direct Welcome → Create account → Sign up → Sign in no-op: `AuthNavGraph` pops to a SignIn destination absent on that direct stack. The separately reviewed [PR1889](https://github.com/ganeshnaik166/equipseva-android/pull/1889) owns its test-first navigation fix; do not fold those code changes into this Welcome PR. Security P1d-1 remote token claim/release and broader app PR1877 remain separate and blocked as recorded in CURRENT_STATE.
