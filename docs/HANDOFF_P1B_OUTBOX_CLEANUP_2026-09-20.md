# P1b — successor-login outbox deletion safety

Status 26 September: **P1b locally accepted for the bounded committed-row deletion guarantee only**. Base: `f7608644728a20bc24aa2fa74e75c3817a461fcd`, on `codex/quality-integration-20260919`. Pre-fix scaffold `1493b870092b934d0bd0b7f10ab05631a0c74137`; fixture correction `0c2572c7c8285a823a9a1f0a35f1b46c2af469cc`; implemented guard `5b5fa68b7475049b2d409f82689cc858d75837ed`; latest bounded setup fixture and source comment `ed9de0c38a13e22d2c0777eaf2321cd08932e77f`. Independent critic and QA each scored this scope **9.6/10**. Draft PR1877 remains blocked by other work. Follow [CURRENT_STATE](CURRENT_STATE.md), [PRODUCT_PLAN](../PRODUCT_PLAN.md) and the [delivery ledger](product-plan/DELIVERY_LEDGER.md).

## Scope and ownership

Coordinator owns an injected Room cleanup adapter, `SignOutCleanup` wiring, the existing local-boundary regression harness and three direct constructor fixtures, documentation, Git and Gradle. The test agent owns only a new `OutboxSignOutCleanerTest` and its uniquely named helper. Architect/QA and critic inspect independently without editing code or running competing builds. Preserve other checkouts and reserve the shared Gradle slot.

Reuse the immutable ticket already captured before the first cleanup suspension. Enter the actual Room write transaction before testing that ticket; only an admitted current ticket can delete. SQLite's writer serialization must keep B's enqueue safe whether B commits before A's admission or queues behind A's admitted deletion. A null or stale ticket retains recovery data. Keep all existing regression assertions, ordinary cleanup, and fresh current-login requests. Do not change Room v5, schema, worker or enqueue contracts.

## Required evidence

New tests precede the fix, using the production generated DAO and file-backed native SQLite under Robolectric. Prove the actual transaction admission barrier in both orders, persisted rereads, direct replacement and observed relogin/ABA, null/signed-out ownership, cancellation/rollback, database failure and fresh retry. Upgrade the older mocked outbox regression to real Room without weakening its assertions. Record any fixture correction separately from behavioural RED. Capture source hashes and exact commands; run targeted and applicable full unit/lint/debug/unsigned-release checks before bounded acceptance. Separate critic and QA reviews must meet the repository rubric; a missing review or test is not a pass.

## Limits

This is deletion safety for successor-login committed work, not complete outbox isolation. Historical rows are unowned; producers, drainers, reads, late callbacks, same-login activity and ciphertext/device verification remain separate work. Unobserved identical identity boundaries remain a ticket limitation. The token-capture regression, global scheduler/photo/preferences/realtime teardown, S3 logout recovery, payment, integrity, dependency, visual, device/provider and signed-release gates remain. Never merge this blocked candidate merely to publish continuity docs. Website work stays paused.

## Source checkpoint and resume recipe

Seven files in the initial scaffold: new `core/sync/OutboxSignOutCleaner.kt`, new `core/sync/OutboxSignOutCleanerTest.kt`, existing `core/auth/SignOutCleanup.kt`, `SignOutCleanupLocalBoundaryRegressionTest.kt`, `SignOutCleanupOwnershipTest.kt`, `SignOutDraftFenceTest.kt`, and `features/hospital/RequestServiceAccountSwitchIntegrationTest.kt`. That historical scaffold deliberately called global `clearAll()` without an ownership check to establish RED. The later `5b5fa68b` implementation admits the real Room write transaction, checks cancellation and the first-operation exact ticket after admission, and calls the existing suspend DAO delete only when current. The write transaction serializes any successor enqueue behind an already admitted A deletion. No schema, DAO API or dependency changed.

The nine new tests use separate real file-backed Room connections, actual SQLite begin/end barriers and persisted reopen. They cover both writer orderings, ordinary deletion/fresh enqueue, missing or stale tickets, observed relogin/raw identity mismatch, cancellation at admission/precommit and trigger-induced SQL failure/retry. The earlier integration test now uses real Room rather than a mocked deletion set; its five original behavioural assertions remain enabled, including token capture. Constructor-only mocking updates in three other fixtures preserve their assertions.

Architect source inspection of declared Room 2.8.4 supported the implemented fix: `database.withTransaction { currentCoroutineContext().ensureActive(); if (ownership.withCurrent(ticket) {}) database.outboxDao().clearAll() }`. `withTransaction` admits the database write transaction before invoking the block; its lock persists across the suspend DAO call. That transaction supplies serialization, while the ownership check rejects a stale departing ticket. Real SQLite tests have exercised both writer orders, rollback, cancellation and fresh writes; SQLCipher/device validation remains separate.

The test author was distinct from the coordinator; the independent QA and critic reviewers were read-only. Their [critic](helper-reviews/codex-20260926/p1b-critic.md) and [QA](helper-reviews/codex-20260926/p1b-qa.md) reports each score the final frozen P1b boundary **9.6/10**, with no mandatory defect within that scope. P1a scores were separate. Do not promote this bounded local result to whole-app or release acceptance.

## Historical 20 September pre-fix execution

The targeted RED command was launched before the usage threshold; it was the only new Gradle run at that checkpoint. Evidence and source snapshots: `outputs/p1b-outbox-cleanup-20260920/red/` outside the repo, with `run-checks.ps1` in its parent. Launch HEAD was `f7608644` with the frozen edits later committed as `1493b870`. No subsequent implementation/full checks had started at that checkpoint. Previous P1a results applied only to their recorded source, not that WIP.

Actual result: **61 tests / 8 suites / 8 failures / 0 errors / 0 skips**, exit 1, BUILD FAILED in 2m22s. All twelve recorded source/test hashes still match the launch manifest. The new class is **9 tests / 6 failures**: five expected assertion failures (B commits first; cancelled late admission; raw identity replacement; null ticket; observed replacement), plus **one unresolved `SQLiteDatabaseLockedException`** in `cancellation after real delete rolls back before disk reread and current retry succeeds`. That locking result is not behavioural RED proof; investigate the fixture/transaction unwind before implementing or claiming the cancellation case. Later branches within a test that failed its first assertion are not independently proven.

The existing real-store local-boundary class remains **5 tests / 2 failures**, the unchanged token-capture and B-outbox survival assertions. The other 47 tests pass. New ordinary/fresh-write, reverse-order transaction and SQL-trigger failure/retry controls pass. Do not call the targeted suite green. The exact command is:

```text
.\gradlew.bat :app:testDebugUnitTest --tests com.equipseva.app.core.sync.OutboxSignOutCleanerTest --tests com.equipseva.app.core.auth.LocalSessionOwnershipTest --tests com.equipseva.app.core.auth.LocalSessionOwnershipFailureTest --tests com.equipseva.app.core.payments.PendingAmcPaymentsCleanupOwnershipTest --tests com.equipseva.app.core.auth.SignOutCleanupLocalBoundaryRegressionTest --tests com.equipseva.app.core.auth.SignOutCleanupOwnershipTest --tests com.equipseva.app.core.auth.SignOutDraftFenceTest --tests com.equipseva.app.features.hospital.RequestServiceAccountSwitchIntegrationTest --no-daemon --console=plain
```

At the 20 September checkpoint, the environment followed the existing JDK17/local Android setup with `PRECHECK_LOOSE=1`; no live account/provider/SQL or release operation was performed. Full unit/lint/debug/release assembly and final critic/QA review had **not run** for P1b. The task's Gradle wrapper completed and its shared build reservation was released. Preserve the failing tests and exact evidence on resume.

## 21–26 September continuation and current evidence

The precommit cancellation fixture originally threw from `setTransactionSuccessful`, which could interrupt SQLite's own commit/end sequence and strand a lock. Test-only `0c2572c7` moved the injected `CancellationException` to the `DELETE FROM outbox` statement after its real SQL execution and before commit. It still asserts exact persisted rollback and a successful current retry. **Corrected behavioural RED:** 61 tests / 7 assertion failures / 0 errors / 0 skips, exit 1 (`red-fixture-corrected-20260921/`); the outbox and token assertions remained enabled. No production guard existed at that run.

The guard at `5b5fa68b` produced the targeted **61/1**, with all nine `OutboxSignOutCleanerTest` cases passing and only the original token-capture regression still failing. Both transaction-admission orders, cancellation, SQL rollback/retry and fresh enqueues were exercised through actual generated Room DAO and native file-backed SQLite (`green-targeted-20260921/`). Twelve source/test hashes matched that targeted launch. This is a scoped deletion-safety result, not full S1.

The first combined full run at `5b5fa68b` ran **3,743 tests / 427 suites / 2 failures / 0 errors / 0 skips**, exit 1. It passed `lintDebug`, `assembleDebug` and `assembleRelease` (unsigned), but the unit result remains failed. One failure was the already-known token capture assertion. The other was a **10-second fixture timeout during `Harness.seed()`'s first cold Room enqueue**, before the B-outbox test body, gate, account switch or safety assertion. The focused version of that case had taken 9.601 seconds; the combined run also ran lint/R8 alongside tests. This result cannot prove the B-row assertion in that full run. Evidence: `full-p1b-20260926/`, with command, XML and source manifest. A new error, deleted test or security failure must never be hidden under this fixture explanation.

Commit `ed9de0c3` only raised the shared synthetic real-I/O fixture's bounded timeout from 10 to 30 seconds and corrected a stale `SignOutCleanup` class comment. All test assertions, names, barriers, production logic and security gates remain. The design-lint ratchet independently passed at `5b5fa68b` with no negative signal above its baseline; no UI source changed afterward.

The final combined command at `ed9de0c3` was `:app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleRelease --continue --no-daemon --console=plain` with the repository's JDK17/Android setup and `PRECHECK_LOOSE=1`. Result: **3,743 tests / 427 suites / 1 failure / 0 errors / 0 skips**. The only failure is the original `SignOutCleanupLocalBoundaryRegressionTest` token-capture assertion: A's sign-out captured B's token instead of A's. The original B-outbox assertion, all nine new Room tests, and the rest of the full suite passed. `lintDebug`, `assembleDebug` and `assembleRelease`/R8 passed, while combined Gradle exited 1 because the unit suite is not green. Twelve source/test SHA-256 hashes match the launch manifest. Evidence, including complete XML, exact command, summary, Gradle log and source snapshots, is outside Git at `C:/Users/lokes/Documents/Codex/2026-09-07/im/outputs/p1b-outbox-cleanup-20260920/full-p1b-final-20260926/`.

The release assembly was unsigned; signing, installation, provider/device, SQLCipher, Play release and user-flow verification did not run. Gradle reported a Crashlytics mapping upload during the release build, but no independent receipt was checked. No live account, production migration or release was performed. The shared Gradle reservation was released after the wrapper exited. Critic and QA independently accepted the bounded P1b deletion safety at **9.6/10 each**. Token capture and the wider S1/S3 ownership boundaries keep the app candidate blocked from `main`.
