# Sharp advisory lockfile update — 26 September 2026

## Resume point

- Branch: `codex/security-sharp-20260926`, isolated worktree `C:/Users/lokes/Documents/Codex/2026-09-07/im/work/equipseva-sharp-security-20260926`.
- Exact fetched `origin/main` base: `6a6b224681d3595b3c358a029e056e74496bac67`.
- Initial implementation commit: `15726a634fe9c6d36516f07c4b92df9314b4718d`. Corrected frozen web code: `13bef61efd9d986d7f8b1b6a238b37d0d6376daf`. Later documentation commits do not change the tested web inputs. Verify the live branch head with Git before resuming.
- Source change: `web/package-lock.json` only. `sharp` and matching `@img/sharp-*` platform packages move from 0.35.3 to 0.35.4; bundled `@img/sharp-libvips-*` moves from 1.3.2 to 1.3.3. The corrected code also preserves the 16 published Linux `libc` selectors (`glibc` or `musl`). The package manifest and all non-Sharp lock entries are unchanged.
- [GHSA-rgj7-g3m4-5g8c](https://github.com/advisories/GHSA-rgj7-g3m4-5g8c) marks Sharp versions below 0.35.4 affected by upstream libheif flaws. The repo's Dependabot [PR1867](https://github.com/ganeshnaik166/equipseva-android/pull/1867) was made from old base `fe463756` and its lockfile diff is not applied as a branch merge.

## Exact local checks

The laptop exposes bundled Node `v24.19.0` but has no bare `npm` on PATH. From `web/`, commands below used `C:/Users/lokes/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/fallback/pnpm.cmd dlx npm@10.8.2` as the `npm` executable prefix. npm generated the Sharp update with `update sharp --package-lock-only --ignore-scripts --no-audit --no-fund`; six unrelated optional Tailwind lock entries generated by that npm invocation were omitted, retaining the base lock's unrelated dependency tree. The initial lock differed at exactly 27 Sharp-related package entries. Independent critic found that the Windows-generated lock had dropped the published `libc` selector from all 16 Linux Sharp/libvips records; the initial **9.2/10 was a hold**, not acceptance. A direct assertion was RED with 16/16 missing selectors. Corrective commit `13bef61e` added exactly these 16 selectors (48 lines), copying the package metadata from the old Dependabot lock. After correction, 16/16 complete Linux package records match that lock and every other package record matches `15726a63`. `git diff --check` passed. Fresh independent review and GitHub Linux CI remain required.

| Command (after executable prefix) | Observed result |
|---|---|
| `ci --no-audit --no-fund` on corrected lock | Exit 0; 101 packages installed |
| `run typecheck` on corrected lock | Exit 0 |
| `test` on corrected lock | Exit 0; DataTable smoke passed |
| `run build` on corrected lock with CI's three placeholder `NEXT_PUBLIC_*` values and telemetry disabled | Exit 0; optimized compilation, TypeScript, static generation and route build completed; local log in `outputs/sharp-security-20260926/build-libc-final.log` |

Reading installed Sharp directly reported `sharp=0.35.4`, `heif=1.23.2`, `vips=8.18.6`. This checks the local Windows binary. GitHub `web` CI uses Node 20/Linux and is the cross-platform gate on [draft PR1885](https://github.com/ganeshnaik166/equipseva-android/pull/1885). Fresh CI, secret-scan, independent critic and QA results must be recorded before calling this slice accepted.

## Boundaries and next action

This resolves the locked transitive Sharp version on the web console branch; it does not claim all dependency alerts, uploaded-image path safety, Android security, a production deploy or an app-wide score. The app candidate and P1d sender work live in other checkouts and are untouched. Fetch before pushing this branch. Wait for PR1885's fresh web and secret-scan CI checks, then obtain separate critic and QA scores for corrected code `13bef61e`. Merge only after applicable gates are green.
