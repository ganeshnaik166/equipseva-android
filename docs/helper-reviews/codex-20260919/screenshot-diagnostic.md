# Bounded screenshot diagnostic — 2026-09-19

Read-only comparison: candidate `50999895` (includes UI checkpoint `4eff9984`) versus main `e4a5e0db`; Compose alignment commit `a83dbedb`. No source edits, Gradle runs, recordings or golden changes performed.

## Evidence and limits

- Hosted run `35436516410`, job `105879988733`, reported **116 tests / 116 failures**. The downloaded report now resolves the failure-type uncertainty: **Total 116, Recorded 0, Added 0, Changed 116, Unchanged 0**. It classifies all 116 as changed renderings against existing references, not missing-reference additions. Sampled stack traces show `java.lang.AssertionError` at the capture calls in `ActiveWorkScreenScreenshotTest.kt:35`, `ConversationsScreenScreenshotTest.kt:35`, `EarningsScreenScreenshotTest.kt:45`, and `HospitalActiveJobsScreenScreenshotTest.kt:41`; these samples do not show a linkage exception.
- Artifact `10581344819` was downloaded and SHA256-verified by the coordinator: `roborazzi-report-35436516410.zip`, 57,406,032 bytes, digest `7eed3d29ed70f26cbe8e5eab79dd1f8fc52479b41c34ca9fcbf4a4362206b430`. This reviewer visually inspected only the two extracted comparison PNGs listed below. The aggregate changed count is report evidence, not visual acceptance of all images.
- `.github/workflows/roborazzi.yml`, the committed golden PNGs, `gradle.properties:54`, and the hand-written `*ScreenshotTest.kt` fixtures are unchanged between the two revisions. The last golden update reachable from main is `d3a9587b` (`test(ux): re-record Roborazzi goldens on Linux CI`). Candidate changes have not been incorporated into the baselines.
- Screenshot setup is unchanged: `app/build.gradle.kts:223–239` includes Android resources and hardware pixel copy; `:276–293` retains the same output directory, preview package/private-preview inclusion, SDK 35, Pixel 5 qualifiers and vanilla Application. `gradle/libs.versions.toml:36–38` still selects Robolectric 4.16.1, Roborazzi 1.74.0 and preview scanner 0.9.3. No source evidence currently points to a changed output path, SDK, device qualifier or recording OS.

## Source changes explaining baseline incompatibility

1. **Shared theme and unchanged baselines.** `Theme.kt:16–42` replaces the Material palette and surface roles; `:63–68` selects the new locale-aware typography. `Typography.kt:13–28` introduces bundled Space Grotesk, Inter and Indic fonts; `:30–50` changes weights, role sizes, line heights, letter spacing and font padding. `Type.kt` replaces static SansSerif styles with Material role getters. `Shape.kt:7–12` replaces uniform 5 dp corners with 8/16/24/28 dp roles. These changes can affect even components whose own source is unchanged. For a small discriminator, `Avatar.kt:81,94,106–115` renders the changed caption/body fonts under EquipSevaTheme; `RequestSentScreenScreenshotTest.kt:34–36` also renders the shared theme explicitly in light mode.
2. **Compose dependency baseline.** `a83dbedb` changes `gradle/libs.versions.toml:8` from BOM 2024.12.01 to 2025.08.00 and `app/build.gradle.kts:328–331` to an enforced platform. This intentionally resolves the observed compile/runtime mismatch. It also changes the resolved rendering dependency set relative to the golden-producing revision, so any visual difference cannot automatically be attributed only to product tokens. The currently sampled hosted assertion failures provide no reason to revert alignment or infer that linkage remains broken.
3. **Platform reproducibility remains a constraint, not an established cause.** Both workflow jobs use Linux/JDK 17, and the workflow explicitly requires Linux-recorded goldens. The file is unchanged. Local Windows functional/UI tests cannot establish equality with Linux goldens; generating Windows goldens would add another uncontrolled rendering difference.

## Two visually inspected hosted comparisons

- `screenshot-samples/RequestSentScreen_with_job_number_compare.png`: reference/new panels both render the request confirmation and the same job number, explanatory steps and navigation actions. The new `View job` action is a lime pill with dark text and an outline, replacing the green rectangular action with white text. Heading/body/button typography and vertical positions change; the job-status sentence wraps at a different word. The success symbol and step icons remain green. These are actual rendered changes consistent with the theme/action migration. No obvious overlap or missing label is visible in this sample; this is not a large-font, dark-mode, locale or interactive accessibility check.
- `screenshot-samples/com.equipseva.app.designsystem.components.AvatarKt.AvatarPreview.Avatar_WITH_BACKGROUND_compare.png`: font shapes and caption wrapping change, the second caption takes an additional line, and the rendered gallery grows in width/height. All five size examples and all five initials examples remain visible; circles retain the legacy green gradient, while typography follows the new shared theme. This is partial visual-theme adoption to review intentionally, not evidence that every legacy color must change. No obvious clipped initials are visible at the displayed scale.

## Remaining verification

The failure-type question is closed: this hosted report contains 116 changed renderings. The correctness question remains open: only **2 of 116** comparisons were visually inspected. Review the remaining renderings, including large-text previews, for unintended clipping, action/label loss, mixed-token treatment and layout regressions. These two samples alone do not establish that all differences are desired or that Compose alignment is visually neutral.

After reviewing and resolving the changes, record approved baselines on Linux and run independent verification against that exact tree. Do not weaken tolerances, remove screenshot cases or replace all baselines merely to clear the current failures. No recording was dispatched and no golden was edited during this diagnostic.

No screenshot acceptance score is claimed; hosted visual comparison remains open.
