# P1d-0 selective main integration — independent QA, 26 September 2026

**Scope and score:** **9.6/10 for the frozen selective port and sender CI contract**, at pushed PR1886 head `df42167d8973372146fe377092526f2c48d96caf`. No mandatory source/workflow defect found in this scope. **Main merge remains pending** until all PR checks, including the currently running Android debug/R8 jobs, finish green. This is not a hosted Supabase deployment, live FCM, shared-device, P1d-1 claim/release, Android candidate, or release score.

Branch `codex/p1d0-main-20260926` started at main `6a6b224681d3595b3c358a029e056e74496bac67` and now includes accepted Sharp main merge `a645a59300c9ab1ec9ce93a16efdcdf3ef6f8f8b`; `git merge-base origin/main HEAD` returned that exact newer main SHA. Local and remote branch heads matched and the worktree was clean. `git diff --check origin/main...HEAD` exited 0.

`git diff --name-status origin/main...HEAD` contains only the four `supabase/functions/send_push_notification/` sender/config/test files, new `.github/workflows/push-sender-contract.yml`, and continuity/review Markdown. No Android, SQL, grants, web dependency or UI file is changed by this PR. Comparing Git blob IDs at HEAD with the locally accepted helper source `85b91ddd88619bb062468d37b0afa63880759056` returned **4/4 exact matches** for `index.ts`, `push_contract.ts`, `push_contract.test.mjs`, and `deno.json`. The helper's blocked P1a/P1b/P1c Android ancestry is absent from the diff.

Independent checks from the function directory on this frozen integration tree:

- Bundled Node 24: `node --test push_contract.test.mjs` — **13 passed, 0 failed, exit 0**.
- Pinned Deno 1.46.3 and 2.9.7: each ran `deno check --no-lock index.ts`, `deno test --no-lock --allow-read push_contract.test.mjs`, and `deno lint index.ts push_contract.ts push_contract.test.mjs` — all six commands **exit 0**; each test run **13 passed, 0 failed**.
- `actionlint 1.7.12 .github/workflows/push-sender-contract.yml` — exit 0. The checkout, setup-node and setup-deno action commit pins each matched their declared upstream release tag by `git ls-remote`. The workflow is path-scoped, read-only, runs from the function directory and sends no secrets to synthetic tests.

At the checked PR1886 SHA, GitHub check-run API reported [Node 24](https://github.com/ganeshnaik166/equipseva-android/actions/runs/36237551176/job/108391982816), [Deno 1.46.3](https://github.com/ganeshnaik166/equipseva-android/actions/runs/36237551176/job/108391982979), [Deno 2.9.7](https://github.com/ganeshnaik166/equipseva-android/actions/runs/36237551176/job/108391982904), [PR Gitleaks](https://github.com/ganeshnaik166/equipseva-android/actions/runs/36237551181/job/108391982732) and [push Gitleaks](https://github.com/ganeshnaik166/equipseva-android/actions/runs/36237523601/job/108391903619) **completed success**. Two Android debug/release R8 jobs were still **in progress** at this check; inspect their final result before merging. The new CI tests the pure sender contract and typechecks the entry point on Linux, but it does not bundle or deploy through Supabase CLI.

The helper's security behavior remains: generic visible push text; fetched recipient and notification IDs; allow-listed kind/validated route ID; structured FCM `UNREGISTERED` classification; no token-only reaping; and sibling send accounting. The port itself does not solve remote token ownership, old-client DELETE-grant conflict, stale-device data after account switching, invalid-token slot exhaustion, replay duplication, or provider/device verification. Keep those gates open as recorded in `docs/HANDOFF_P1D0_MAIN_INTEGRATION_2026-09-26.md`.
