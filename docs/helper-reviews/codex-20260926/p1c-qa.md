# P1c departing token capture and draft ownership — independent QA

Reviewed 26 September 2026. Baseline P1b checkpoint: `11b43ee97a15afde2cf62d796f8905cbe33164dc`. Frozen P1c executable implementation: `ed7a5397`; final test-only fixture and disk controls: `04aa5729f8d2ec7a1d5e3381178f2a334904c61a` on `codex/quality-integration-20260919`. I reviewed the source diff, test fixtures, saved Gradle XML/logs and source hashes independently. I did not edit repository files or run Gradle.

**Scoped QA score: 9.6/10** for binding sign-out token capture and draft deletion to the first-operation exact login ticket, including cancellation behavior. Applicable rubric dimensions: correctness 9.7, security/privacy 9.6, recovery/data 9.6, performance/operations 9.5. UI, accessibility and visual dimensions do not apply to this storage/orchestration slice. There is no unresolved mandatory defect within this declared *local* boundary. This is not a score for remote token removal, whole S1, main integration or release.

The sign-out path checks cancellation, captures one exact ticket, synchronously fences only that login's draft lease, and reads the installation token before its first draft disk suspension. The registrar validates the ticket on both sides of its suspending DAO read and derives the revocation owner from the ticket rather than from a later live account. The draft clear checks the same ticket inside the admitted DataStore edit. A stale A can neither fence B's lease nor erase B's committed draft; observed A→B→A and same-owner new-session boundaries invalidate the old exact ticket. Cancellation escapes ordinary-error best effort and noncooperative DAO/network completion paths.

Evidence and provenance:

- Test-first API RED was a `compileDebugUnitTestKotlin` failure with zero tests because ticketed APIs did not yet exist; it is contract evidence, not behavioral RED. Four added cancellation cases then produced behavioral RED **47 tests / 7 suites / 2 failures** on the intermediate implementation: a cancelled no-ticket sign-out entered global wipes, and a late DAO ordinary failure was swallowed after cancellation. The unchanged focused set passed **47/0** after the cancellation fix.
- A first full run at `ed7a5397` reached **3,761/2**. Both failures were in `RequestServiceAccountSwitchIntegrationTest`: its relaxed ownership mock returned no ticket, so the new guarded draft clear rightly did nothing. Commit `04aa5729` replaced that fixture with a real monitor using the same A/B auth and raw identity and added four file-backed ticketed draft tests. It changed no executable production logic or original account-switch assertions.
- The follow-up focused command passed **59 tests / 9 suites / 0 failures, errors or skips**, including the eight repaired account-switch cases, ten new token-capture cases and four real-disk ticketed cases.
- Final combined command at `04aa5729`: `gradlew.bat :app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleRelease --continue --no-daemon --console=plain`. XML/results independently sum to **3,765 tests / 429 suites / 0 failures / 0 errors / 0 skips**; Gradle exit 0. Lint completed with 0 errors, 91 warnings and 2 hints. Debug and unsigned release assembly, including R8, completed. All 13 captured source/test SHA-256 entries match current files, and `git diff --check` is clear. Evidence: `full-p1c-final-20260926/{summary.json,results.json,source-hashes.json,xml/,gradle.log}` under this output directory.
- The token tests use a controlled suspending DAO and synthetic Supabase Auth/MockEngine HTTP. They assert that null/stale tickets never read a new token, direct and same-owner replacement invalidate a held read, observed ABA cannot revive A, the HTTP DELETE is filtered by captured user and token, ordinary capture retains the installation token, and DAO/network cancellation propagates. The local-boundary and account-switch suites keep their B-work survival and ordinary controls.
- The four new draft tests wrap an actual per-test Preferences DataStore file. They prove A's persisted draft exists before ordinary ticketed clear, unrelated preferences survive, B commits before stale A admission, cancellation after the real edit transform rolls back, and post-transform I/O failure rolls back. Each decisive result is reread through a new DataStore delegate after the writer closes. The account-switch suite additionally proves the actual sign-out sequence clears A's file-backed draft and permits B or a fresh A login.

**Mandatory wider blocker:** tracked migration `20260428320000_security_revoke_delete_grants.sql` revokes authenticated/anon `DELETE` on `public.device_tokens`. The client still attempts table DELETE during registration and sign-out. If that grant is deployed, capture can be locally correct while remote release fails and registration upsert can be skipped. No live grant, real FCM delivery or device test was performed; the remote state is unverified. P1d needs a separate least-privilege, versioned claim/release contract and sender review. Do not restore broad client DELETE.

Other exclusions: the no-argument registrar convenience path is not ticketed; later global router/photo/preferences/realtime wipes, historical data, producers/readers, final SDK logout, S3 recovery, provider/device behavior and signed release are outside this score. A pre-existing registrar comment about `send_push` dedupe conflicts with the separate P1d source investigation and should be corrected there. The broad candidate remains subject to those gates despite this green local suite.
