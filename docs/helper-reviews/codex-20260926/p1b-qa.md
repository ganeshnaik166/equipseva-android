# P1b outbox deletion safety — independent QA

Reviewed 26 September 2026. Frozen code/test HEAD: `ed9de0c38a13e22d2c0777eaf2321cd08932e77f` on `codex/quality-integration-20260919`. I reviewed the diff from `f7608644`, production source, generated-DAO test fixture, saved Gradle XML/logs and source hashes. I did not edit the repository or run Gradle.

**Scoped QA score: 9.6/10 for successor-login Room outbox deletion safety.** Applicable rubric dimensions: correctness 9.7, security/privacy 9.6, recovery/data 9.6, performance/operations 9.5. UI, accessibility, locales and visual consistency are outside this storage-only slice. The guard is acceptable for this bounded deletion claim; this score does not accept S1, app integration or release.

The adapter captures the departing ticket before suspension, takes a real Room write transaction, checks coroutine cancellation and the exact current ticket after admission, and calls the existing suspend `clearAll()` inside that transaction. A stale or null ticket cannot delete. The transaction keeps a competing enqueue serialized while the delete suspends. It does not account-isolate historical rows, producers, readers or workers.

Evidence:

- Corrected baseline RED after test-fixture commit `0c2572c7`: 61 tests / 8 suites / 7 failures / 0 errors / 0 skips. Five new ownership assertions, the old B-outbox regression and the pre-existing token-capture regression failed. The cancellation fixture was moved from SQLite commit interception into the actual DELETE statement, so it no longer leaked a lock; no safety assertion was weakened.
- Targeted guarded run: 61 tests / 8 suites / 1 failure; all nine `OutboxSignOutCleanerTest` cases and the B-outbox regression passed. Only the token-capture assertion failed.
- Final full command at `ed9de0c3`: `gradlew.bat :app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleRelease --continue --no-daemon --console=plain`. XML sums to **3,743 tests / 427 suites / 1 failure / 0 errors / 0 skips**. All nine new real Room/native SQLite cases pass. In the five-case local-boundary regression, B-outbox survival, B-payment survival and ordinary A cleanup pass; the sole failure is `departing owner is captured before a delayed draft disk clear can observe B`, which still captures B's token. Gradle exits 1 on that failure. Lint completed with 0 errors, 91 warnings and 2 hints; debug and unsigned release assemblies, including R8, completed.
- Both writer orderings use separate file-backed Room connections and actual SQLite begin/end observation. B commits before A admission in one case; in the reverse case A's DELETE is observed before commit, B's enqueue queues behind it, and a disk reopen retains B. Other cases cover ordinary cleanup/fresh B write, null/signed-out/unknown ownership, observed B/ABA/new-session and raw replacement, cancellation while admission is blocked, rollback after real DELETE, and SQL-trigger failure/retry. No test is skipped.
- The first full attempt was 3,743/2 because the first local-boundary test timed out during its initial Room seed, before any test gate or safety assertion. Its XML stack identified the fixture's 10-second I/O timeout at the first enqueue. Commit `ed9de0c3` increased only that fixture's bounded real-I/O timeout to 30 seconds and corrected stale KDoc. The final full run passed that test; executable production logic and assertions were unchanged by this follow-up.
- The final artifact's 12 SHA-256 source/test entries match current files; its base SHA is `ed9de0c3`. `git diff --check` reported no whitespace errors. Evidence: `full-p1b-final-20260926/{summary.json,results.json,source-hashes.json,xml/,gradle.log}` under this output directory.

**Mandatory blocker outside the scored slice:** token capture still reads B after A's draft disk suspension. The full suite is red and app/main integration must remain blocked until that regression and the remaining S1/S3 ownership work are resolved. These tests use native plain SQLite with the production generated DAO, not the production SQLCipher open-helper or a device/provider run. No signed release, field usability or whole-app security acceptance follows from this review.
