// Shim: some generated founder pages import "@/components/founder/DataTable",
// but the component lives at "@/components/DataTable". Re-export keeps the
// path valid without touching the pages.
export { DataTable } from "../DataTable";
export type { Column } from "../DataTable";
