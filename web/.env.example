# Public — safe to ship in client bundle (anon key respects RLS).
NEXT_PUBLIC_SUPABASE_URL=https://eyswaywvtartpvtoxtdr.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=replace_with_anon_key_from_supabase_dashboard_settings_api
NEXT_PUBLIC_FOUNDER_EMAIL=ganesh1431.dhanavath@gmail.com

# DO NOT add SUPABASE_SERVICE_ROLE_KEY here — the web app must NEVER
# bypass RLS. All founder-only data flows through SECURITY DEFINER RPCs
# that gate on is_founder() via the JWT email claim.
