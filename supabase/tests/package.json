{
  "name": "equipseva-supabase-focused-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:evidence && npm run test:cron && npm run test:snapshot && npm run test:contract",
    "test:evidence": "node evidence_authorization.test.mjs",
    "test:cron": "node --experimental-vm-modules --test cron_observability.test.mjs",
    "test:snapshot": "node --test storage_snapshot_timeout.test.mjs",
    "test:contract": "node evidence_client_contract.test.mjs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8"
  }
}
